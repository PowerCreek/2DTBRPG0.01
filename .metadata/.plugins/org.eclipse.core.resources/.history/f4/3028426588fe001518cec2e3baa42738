package com.mygdx.game;

import com.badlogic.gdx.ApplicationAdapter;
import com.badlogic.gdx.Gdx;
import com.badlogic.gdx.graphics.GL20;
import com.badlogic.gdx.graphics.OrthographicCamera;
import com.badlogic.gdx.graphics.g2d.SpriteBatch;
import com.mygdx.game.pFunctional.Input;
import com.mygdx.game.pFunctional.RunGame;

public class MyGdxGame extends ApplicationAdapter {
	SpriteBatch batch;
	
	RunGame RG = new RunGame();
	Input input = new Input();
	
	OrthographicCamera cam = new OrthographicCamera(1280,720);
	
	@Override
	public void create () {
		RG.init();
		batch = new SpriteBatch();
		input.apply();
		
	}

	public void update(){
		RG.screen.set(0, Gdx.graphics.getWidth());
		RG.screen.set(1, Gdx.graphics.getHeight());
	}
	
	@Override
	public void render () {
		update();
		Gdx.gl.glClearColor(1, 0, 0, 1);
		Gdx.gl.glClear(GL20.GL_COLOR_BUFFER_BIT);
		batch.begin();
		RG.render(batch,input);
		batch.end();
	}
}
